[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "prevalidate"
version = "0.1.0"
description = """
    A language-agnostic dry-run tool that users can run on a cluster before 
    `sbatch`, that profiles their task and flags cluster-unfriendly behavior.
"""
readme = "README.md"
authors = [
  {name = "Florence Bockting", email = "florence.bockting@aalto.fi"}
]
maintainers = [
  {name = "Florence Bockting", email = "florence.bockting@aalto.fi"}
]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Typing :: Typed",
]
license = {text = "MIT"}
dependencies = [
  "typer",
  "rich",
  "psutil",
  "nvidia-ml-py",
]
requires-python = ">= 3.12"

[dependency-groups]
dev = [
    { include-group = "lint" },
    { include-group = "test" },
    { include-group = "typecheck" },
]
lint = [
    "ruff",
]
test = [
    "coverage",
    "pytest",
]
typecheck = [
    "ty",
]
docs = [
    "zensical",
    "mkdocstrings-python",
]

[project.urls]
bugs = "https://github.com/florence-bockting/prevalidate/issues"
changelog = "https://github.com/florence-bockting/prevalidate/releases"
# documentation = "https://florence-bockting.github.io/prevalidate/"
homepage = "https://github.com/florence-bockting/prevalidate"

[project.scripts]
prevalidate = "prevalidate.cli:app"

[tool.ty]
# All rules are enabled as "error" by default; no need to specify unless overriding.
# Example override: relax a rule for the entire project (uncomment if needed).
# rules.TY015 = "warn"  # For invalid-argument-type, warn instead of error.

[tool.ruff]
line-length = 80

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]

[tool.coverage.run]
branch = true
parallel = true
source = ["src/", "tests/"]

[tool.coverage.report]
show_missing = true
skip_covered = true
fail_under = 10 # TODO: increase to 80
exclude_also = [
    "if TYPE_CHECKING:",
    "if typing.TYPE_CHECKING:",
    "@overload",
    "@typing.overload",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
    "raise NotImplementedError",
    "\\.\\.\\.",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv]
package = true
default-groups = ["dev"]
